server:
  port: 8080
  servlet:
    context-path: /palm/bank

spring:
  application:
    name: palm-bank-life-web
  datasource:
    url: jdbc:mysql://localhost:3306/palm-bank?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  redis:
    redisson:
      config: |
        singleServerConfig:
          address: "redis://localhost:6379"
          database: 0
  # 解决Swagger路径匹配问题
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher

mybatis:
  mapper-locations: classpath*:mapper/*.xml
  type-aliases-package: com.life.bank.palm.dao
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    map-underscore-to-camel-case: true

# Knife4j配置
knife4j:
  enable: true
  production: false  # 开发环境设为false，生产环境设为true会关闭文档
  basic:
    enable: false    # 是否开启认证
    username: admin
    password: 123456

# Springfox配置
springfox:
  documentation:
    swagger-ui:
      enabled: true
      path: /doc.html

# AI配置
#ai:
#  deepseek:
#    api-key: sk-567887f5f72c482ea30b729e55a5b13c  # 实际使用时替换为真实的API Key
#    api-url: https://api.deepseek.com/v1/chat/completions
#    model: deepseek-chat
#    temperature: 0.7
#    max-tokens: 2048

ai:
  openai:
    api-key: "sk-567887f5f72c482ea30b729e55a5b13c" # 安全起见，从系统环境变量读取
    base-url: https://api.deepseek.com/v1
    chat:
      options:
        model: deepseek-chat
        temperature: 0.5